{
  "name": "transaction-desk-e2e",
  "private": true,
  "description": "Browser end-to-end tests. Kept out of client/ and server/ so the test runner and its browsers are never a dependency of anything that ships.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "seed": "node ../server/scripts/seed-test-env.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
